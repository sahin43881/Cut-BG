# Public URL of the deployed Cloudflare Worker.
# Used by the browser to log usage and check the per-IP rate limit.
# In production, set this in Cloudflare Pages env vars.
NEXT_PUBLIC_WORKER_URL=https://cutbg-api.your-subdomain.workers.dev
